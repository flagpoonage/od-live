

export function renderButtonComponent (buttonId: string, container: HTMLElement, buttonContent: string) {

  // Render a new button inside the container. If it already exists, only update its content.

}